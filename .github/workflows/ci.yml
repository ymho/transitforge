name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Run TypeScript tests
        run: npm test

      - name: Build viewer
        run: npm run build
        env:
          VITE_MAPBOX_ACCESS_TOKEN: test-token-for-build-only

      - name: Run Python tests
        run: python3 -m unittest discover -s tests -v

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.12.2

      - name: Check Terraform formatting
        run: terraform fmt -check -recursive infra/terraform

      - name: Validate Terraform bootstrap
        working-directory: infra/terraform/bootstrap
        run: |
          terraform init -backend=false -input=false
          terraform validate

      - name: Validate Terraform dev environment
        working-directory: infra/terraform/environments/dev
        run: |
          terraform init -backend=false -input=false
          terraform validate

  deploy:
    needs: test
    if: >-
      github.ref == 'refs/heads/main' &&
      (github.event_name == 'workflow_dispatch' || github.event_name == 'push')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: dev
    permissions:
      contents: read
      id-token: write
    concurrency:
      group: transitforge-dev
      cancel-in-progress: false
    env:
      AWS_REGION: ap-northeast-1
      TF_IN_AUTOMATION: "true"
      TF_INPUT: "false"
      TF_VAR_basic_auth_credentials_sha256: ${{ secrets.BASIC_AUTH_CREDENTIALS_SHA256 }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Build viewer with deployment token
        run: npm run build
        env:
          VITE_MAPBOX_ACCESS_TOKEN: ${{ secrets.VITE_MAPBOX_ACCESS_TOKEN }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.12.2

      - name: Configure AWS credentials with GitHub OIDC
        uses: aws-actions/configure-aws-credentials@v6.1.2
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          mask-aws-account-id: true

      - name: Apply Terraform
        working-directory: infra/terraform/environments/dev
        run: |
          terraform init \
            -backend-config="bucket=transitforge-terraform-state-911167912657" \
            -backend-config="region=${AWS_REGION}"
          terraform plan -out=dev.tfplan
          terraform apply -auto-approve dev.tfplan

      - name: Read deployment outputs
        id: terraform
        working-directory: infra/terraform/environments/dev
        run: |
          echo "website_bucket=$(terraform output -raw website_bucket_name)" >> "$GITHUB_OUTPUT"
          echo "distribution_id=$(terraform output -raw cloudfront_distribution_id)" >> "$GITHUB_OUTPUT"

      - name: Build runtime train index
        run: |
          aws s3 cp \
            "s3://${{ steps.terraform.outputs.website_bucket }}/viewer-input/train_index.json" \
            /tmp/transitforge-train-index.json
          node tools/build_runtime_train_index.mjs \
            /tmp/transitforge-train-index.json \
            /tmp/transitforge-train-runtime-index.json

      - name: Deploy viewer assets
        run: |
          aws s3 sync dist "s3://${{ steps.terraform.outputs.website_bucket }}/" \
            --delete \
            --exclude "viewer-input/*" \
            --exclude "api/*"
          aws s3 cp \
            /tmp/transitforge-train-runtime-index.json \
            "s3://${{ steps.terraform.outputs.website_bucket }}/viewer-input/train_runtime_index.json" \
            --content-type "application/json; charset=utf-8" \
            --cache-control "no-cache"
          aws cloudfront create-invalidation \
            --distribution-id "${{ steps.terraform.outputs.distribution_id }}" \
            --paths "/*"
